# Operating system (tiny scheduler tests)

Run locally (Windows):

Using Batch (recommended):
```powershell
build.bat    # compile once
run.bat      # build if needed, then run
```

Using PowerShell:
```powershell
.\







- For IntelliJ: add `src/gson-2.10.1.jar` as a library on the module classpath and create a Run configuration for `Test`.- The project includes `src/gson-2.10.1.jar`; scripts add it to the classpath.Notes:```.\
un.ps1 -Build  # force rebuild then runun.ps1     # runs (compiles if needed)